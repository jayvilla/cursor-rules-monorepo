/**
 * Main entry point for @org/shared-dto library
 * 
 * Usage:
 *   import { loginSchema, type LoginInput } from '@org/shared-dto';
 *   import { createAuditEventSchema } from '@org/shared-dto';
 */

// Export all Zod schemas
export * from './schemas';

// Export TypeScript types (if you create a separate types directory)
// export * from './types';

// Export utilities (if you create utility functions)
// export * from './utils/zod-helpers';
// export * from './utils/validation';

